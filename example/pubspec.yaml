name: example
description: A project that showcases usage of openapi_gen
publish_to: none
environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  gio: ^0.2.2
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.6.0
  lints: ^6.1.0
  json_serializable: ^6.10.0
  openapi_gen:
    path:
      ../
